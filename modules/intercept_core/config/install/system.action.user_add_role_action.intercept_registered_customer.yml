langcode: en
status: true
dependencies:
  config:
    - user.role.intercept_registered_customer
  module:
    - user
id: user_add_role_action.intercept_registered_customer
label: 'Add the InterCEPT Registered Customer role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: intercept_registered_customer
