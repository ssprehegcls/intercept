langcode: en
status: true
dependencies:
  config:
    - user.role.intercept_registered_customer
  module:
    - user
id: user_remove_role_action.intercept_registered_customer
label: 'Remove the InterCEPT Registered Customer role from the selected user(s)'
type: user
plugin: user_remove_role_action
configuration:
  rid: intercept_registered_customer
